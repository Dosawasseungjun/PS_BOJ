#include <bits/stdc++.h>
#define fast_io cin.tie(NULL); ios_base::sync_with_stdio(false);
using namespace std;

void solv(){
    int N; cin >> N;
    cout << (N == 2 ? "kang\n" : "chan\n");
}

int main(){
    fast_io
    int tt; cin >> tt;
    while(tt--) solv();
}